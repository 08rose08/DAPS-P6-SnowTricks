
<div class="d-flex flex-column align-items-center my-4">
    {% for comment in trick.comments %}
        <div class="d-flex flex-row justify-content-start col-sm-12 col-md-9 col-lg-6 my-1">
            <div class="col-2 m-auto p-2">
                <img class="user-img" src="{{ comment.user.image }}" alt="">
            </div>
            <div class="flex-fill d-flex flex-column border border-secondary rounded pt-2 px-2">
                <p><em>Le {{ comment.createdAt|date('d/m/Y à H:i') }} {{ comment.user.name }} commente :</em></p>
                <p>{{ comment. content}}</p>
            </div>
        </div>
    {% endfor %}

</div>

